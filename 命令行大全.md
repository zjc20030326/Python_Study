# 命令行大全

## Conda

```bash
conda create -n env_name python=3.xx    # 创建环境指定Python版本
conda create -p ./env python=3.xx       # 在指定路径创建环境
conda activate env_name                 # 激活环境
conda deactivate                        # 退出环境

conda list                              # 查看当前环境已安装包
conda list --export > requirements.txt  # 导出当前环境包信息
conda install --file requirements.txt   # 根据文件批量安装包
conda install numpy=x.xx                # 安装特定版本的包

conda env list                          # 查看所有虚拟环境
conda remove -n env_name --all          # 删除指定名称环境
conda env remove -p ./env               # 删除指定路径环境
conda clean --all                       # 清理缓存释放空间

conda update --all                      # 更新当前环境所有包
conda update conda                      # 更新conda自身
conda update anaconda                   # 更新anaconda

# 镜像源配置
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --set show_channel_urls yes
conda config --show channels            # 查看当前配置的源
```

## Python & Pip

```bash
python xx.py                  # 运行当前目录脚本
python --version              # 查看Python版本

pip install pandas            # 安装包
pip install -r req.txt        # 安装依赖文件
pip list                      # 查看已安装包
pip uninstall pandas          # 卸载包
```

## Linux

```bash
ls -lh                        # 列出文件(详细信息+易读大小)
ls -a                         # 列出所有文件(包含隐藏文件)
cd ~/pde_learning             # 进入指定目录
cd ..                         # 返回上一级
pwd                           # 显示当前完整路径

mkdir folder                  # 创建文件夹
cp source target              # 复制文件
cp -r folder_a folder_b       # 复制文件夹
mv old.py new.py              # 重命名或移动文件
rm file.txt                   # 删除文件
rm -rf folder                 # 强制递归删除文件夹

cat file.txt                  # 查看文件内容
code .                        # 用VS Code打开当前目录
code ~/.bashrc                # 用VS Code打开配置文件
source ~/.bashrc              # 重新加载配置文件

nvidia-smi                    # 查看GPU显存状态
chmod +x script.sh            # 添加可执行权限
```

## Git

![工作流程](https://www.runoob.com/wp-content/uploads/2015/02/git-command.jpg)

### 常用命令

```bash
mkdir       # 创建仓库目录
cd <name>                     # 进入仓库目录
git init                      # 初始化仓库
git clone <url>               # 克隆远程仓库
git status                    # 查看文件状态

git add <file>                # 添加指定文件到暂存区
git add .                     # 添加所有更改到暂存区
git commit -m "xx"            # 提交修改并添加说明
git push origin main          # 推送到远程分支
```

### 提交与修改

```bash
git diff                   #比较文件的不同，即暂存区和工作区的差异
git difftool               #使用外部差异工具查看和比较文件的更改
git range-diff               #比较两个提交范围之间的差异
git reset                   #回退版本
git rm                       #将文件从暂存区和工作区中删除
git mv                       #移动或重命名工作区文件
git notes                   #添加注释
git checkout               #分支切换
git switch                    #更清晰地切换分支
git restore                   #恢复或撤销文件的更改
git show                   #显示 Git 对象的详细信息
```

### 提交日志

```bash
git log                       #查看历史提交记录
git blame <file>           #以列表形式查看指定文件的历史修改记录
git shortlog               #生成简洁的提交日志摘要
git describe               #生成一个可读的字符串，该字符串基于 Git 的标签系统来描述当前的提交
```

### 远程操作

```bash
git remote                   #远程仓库操作
git fetch                   #从远程获取代码库
git pull                   #下载远程代码并合并
git push                   #上传远程代码并合并
git submodule               #管理包含其他 Git 仓库的项目
```

### 创建分支

```bash
git branch <branchname>       #创建分支
git checkout -b <branchname>  #创建新分支并切换到该分支
git checkout <branchname>     #切换分支命令
```

### 查看分支

```bash
git branch                    #查看所有分支
git branch -r                 #查看远程分支
git branch -a                 #查看所有本地和远程分支
```

### 合并分支

```bash
git merge <branchname>        #将其他分支合并到当前分支
```

### 删除分支

```bash
git branch -d <branchname>    #删除本地分支
git branch -D <branchname>    #强制删除未合并的分支
git push origin --delete <branchname> #删除远程分支
```

### 查看提交历史

```bash
git log                       #查看历史提交记录
git blame <file>              #以列表形式查看指定文件的历史修改记录
git log [选项] [分支名/提交哈希]
```

* p：显示提交的补丁（具体更改内容）
* oneline：以简洁的一行格式显示提交信息
* graph：以图形化方式显示分支和合并历史
* decorate：显示分支和标签指向的提交
* author=<作者>：只显示特定作者的提交
* since=<时间>：只显示指定时间之后的提交
* until=<时间>：只显示指定时间之前的提交
* grep=<模式>：只显示包含指定模式的提交消息
* no-merges：不显示合并提交
* stat：显示简略统计信息，包括修改的文件和行数
* abbrev-commit：使用短提交哈希值
* pretty=<格式>：使用自定义的提交信息显示格式
  
```bash
git blame [选项] <文件路径>
```

* -L <起始行号>,<结束行号>：只显示指定行号范围内的代码注释
* -C：对于重命名或拷贝的代码行，也进行代码行溯源
* -M：对于移动的代码行，也进行代码行溯源
* -C -C 或 -M -M：对于较多改动的代码行，进行更进一步的溯源
* --show-stats：显示包含每个作者的行数统计信息
  
### 恢复和回退

```bash
git checkout      #切换分支或恢复文件到指定提交。
git reset         #重置当前分支到指定提交（软重置、混合重置、硬重置）。
git revert        #创建一个新的提交以撤销指定提交，不改变提交历史。
git reflog        #查看历史操作记录，找回丢失的提交。
```

### 标签管理

```bash
git tag (-a)<tagname>              #创建标签(-a添加注释)
git tag                            #查看所有标签
git push origin <tagname>          #推送标签到远程仓库
git push origin --tags             #推送所有标签到远程仓库
git tag -d <tagname>               #删除本地标签
git push origin --delete <tagname> #删除远程标签
git tag -a <tagname> -m "message"  #创建带有注释的标签
git show <tagname>                 #查看标签的信息
```

### Git进阶操作

<https://www.runoob.com/git/git-advance.html>

### Git 远程仓库(Github)

```bash
git remote add [shortname] [url]   # 添加远程仓库
git remote -v                      # 查看远程仓库信息
ssh-keygen -t rsa -C "2033897033@qq.com"  # 生成SSH密钥ssh
ssh -T git@github.com                # 测试SSH连接
git fetch #从远程仓库下载新分支与数据
git merge #从远端仓库提取数据并尝试合并到当前分支
git push [alias] [branch]
# 将本地分支的更新推送到远程仓库的对应分支
git remote rm [别名]          # 删除远程仓库
```
